---
alwaysApply: true
---

# Shopify Online Store 2.0 主题开发通用规范

## 项目结构

```
├── assets/          # CSS, JS, 图片等静态资源
├── config/          # theme.json 配置文件
├── layout/          # 基础布局模板 (theme.liquid, password.liquid)
├── locales/         # 多语言翻译文件 (*.json = storefront, *.schema.json = 编辑器)
├── sections/        # 可视化编辑 sections
├── snippets/        # 可复用代码片段
├── templates/       # JSON 页面模板
└── templates/cart/  # 购物车相关模板
```

## 核心开发约定

1. **优先新增代码**：新增 CSS 规则覆盖 > 修改已有样式；新增 snippet > 修改已有组件
2. **不动第三方应用文件**：`pf-*` / `pagefly-*`（PageFly）、`locksmith*`、`ssw-*` / `swym*`（评论/愿望单）、`widget-*`（第三方小部件）等由应用管理的文件禁止修改
3. **不动应用注入代码**：Locksmith / Yoast SEO / Klaviyo 等应用注入的代码片段保持原样
4. **`settings_data.json` 禁止手动编辑**，由 Shopify 自动生成

## 代码编辑风格

实现任何功能时，**优先选择"新增代码"方案，而非"修改已有代码"方案**。

**决策原则**：
1. **优先新增**：先思考能否通过新增代码（CSS、class、组件、函数）实现
2. **其次修改**：只有当新增代码无法实现时，才考虑修改已有代码
3. **保持原样**：尽量不动原有代码，减少 git diff 中的修改行数

| 需求 | 优先（新增） | 避免（修改） |
|------|-------------|-------------|
| 修改样式 | 新增 CSS 规则覆盖 | 修改原有样式定义 |
| 添加行为 | 新增 class + CSS | 修改 JS 逻辑 |
| 调整布局 | 新增 CSS 规则 | 修改 DOM 结构 |
| 添加功能 | 新增组件/snippet | 修改已有组件 |

**目标**：让每次 git diff 主要显示新增的代码行，更容易追踪变更历史。

## 文件命名规范

- 变量使用下划线命名法: `my_variable`
- Sections: `section-name.liquid`
- Snippets: `snippet-name.liquid`（小写字母 + 连字符）
- Assets: `section-name.css`, `section-name.js`
- JSON 模板:
  - 默认模板: `product.json`, `collection.json`, `page.json`
  - 专属模板: `product.{handle}.json`（如 `product.delta-pro-ultra.json`）
  - 活动模板: `page.{campaign-name}.json`

## Snippet 开发规范

### 命名前缀约定

| 前缀 | 用途 | 是否可修改 |
|------|------|-----------|
| `atb-*` | Add-to-build / 产品组件 | 可以 |
| `icon-*` | SVG 图标 | 可以 |
| `product-*` | 产品卡片和详情 | 可以 |
| `cart-*` | 购物车相关 | 谨慎修改 |
| `mega-menu-*` | 导航菜单 | 谨慎修改 |
| `widget-*` | 第三方小部件 | 避免修改 |
| `pf-*` / `pagefly-*` | PageFly 生成 | 禁止修改 |
| `locksmith*` | Locksmith 应用 | 禁止修改 |
| `ssw-*` / `swym*` | 评论/愿望单 | 禁止修改 |

### 参数传递

```liquid
{% render 'product-card-item', product: product, show_price: true %}
```

### 新建 Snippet 约定

- 文件名使用小写字母和连字符
- 在文件顶部用注释说明用途和参数
- 保持单一职责，一个 snippet 只做一件事

## Section 开发规范

### Schema 结构

每个 section 必须包含 `{% schema %}` 块：

```json
{
  "name": "Section 名称",
  "settings": [],
  "blocks": [],
  "presets": [{ "name": "预设名称" }]
}
```

### 编码规范

- 使用 `{% render 'snippet-name' %}` 引入 snippets，**禁止** `{% include %}`
- Section 设置通过 `{{ section.settings.xxx }}` 访问
- Block 循环使用 `{% for block in section.blocks %}`
- 使用 `{{ content_for_header }}` 和 `{{ content_for_layout }}`
- 图片使用 `loading="lazy"` 和 Shopify 图片 CDN URL filter
- 使用 `{{ 'file.css' | asset_url | stylesheet_tag }}` 加载样式

### 样式隔离

- Section 级样式用 `#shopify-section-{{ section.id }}` 作用域选择器
- 避免全局样式污染其他 sections

## JSON 模板规范

```json
{
  "sections": {
    "section-id": {
      "type": "section-file-name",
      "settings": {},
      "blocks": {}
    }
  },
  "order": ["section-id"]
}
```

- `order` 数组决定 sections 的显示顺序
- 新增 section 到模板时，确保对应的 `.liquid` 文件已存在
- JSON 模板修改后需在 Shopify 主题编辑器中验证

## Theme Assets 开发规范

### 核心文件（谨慎修改）

| 文件 | 用途 |
|------|------|
| `theme.js` | 主题核心 JS，修改需全面测试 |
| `theme.css` | 主题基础样式 |
| `core.css` | 核心布局样式 |
| `custom.css` | 自定义样式覆盖（优先在此添加新样式） |
| `vendor.js` | 第三方库打包 |

### 新增样式的优先顺序

1. **首选**: 在 `custom.css` 末尾添加新 CSS 规则覆盖
2. **其次**: 创建独立 CSS 文件（如 `my-feature.css`）并在 section 中引入
3. **避免**: 直接修改 `theme.css` 或 `core.css`

### JS 开发约定

- 新功能优先用原生 JS 或 Alpine.js（`x-data` / `x-bind` / `x-on`）
- jQuery 仅用于兼容已有代码
- 全局命名空间: `window.theme` 对象
- 监听 `shopify:section:load` 等 Shopify 编辑器事件确保编辑器兼容

## 响应式开发规范

### CSS 断点

```css
@media screen and (max-width: 749px) { }   /* 移动端 */
@media screen and (max-width: 989px) { }   /* 平板 */
@media screen and (min-width: 750px) { }   /* 小桌面 */
@media screen and (min-width: 990px) { }   /* 大桌面 */
```

### 开发原则

- **PC端修改**: 直接修改主 CSS，不需要 `@media (min-width)` 包裹
- **移动端修改**: 使用 `@media (max-width: xxx)` 包裹，确保仅在移动端生效
- **互不影响**: 修改 PC 端时确保样式不被移动端媒体查询覆盖；修改移动端时使用明确断点

## 图片和性能

- 使用 Shopify CDN 尺寸后缀: `_720x.png`, `_1500x.png`
- 图片标签加 `loading="lazy"`
- CSS/JS 文件通过 `{{ 'file.css' | asset_url }}` 引用

## Figma 设计稿对接

1. 打开 Figma 设计稿获取节点 ID
2. 使用 `Figma_Desktop_get_design_context` 获取设计上下文和样式
3. 使用 `Figma_Desktop_get_variable_defs` 获取设计系统变量（颜色、字体等）
4. 将 Figma 样式转换为 CSS 类或 Liquid 样式

## Chrome DevTools 调试规范

### 调试页面说明

- **必须使用在线预览链接**进行调试，店铺域名格式: `{国家代码小写}.shopify.com`（如 `ca.shopify.com`、`ng.shopify.com`）
- **禁止使用本地地址** `http://127.0.0.1:port` 作为调试页面
- 用户会手动提前打开预览页面并保持在浏览器最后一个标签页

### 页面选择流程

1. `list_pages` → 获取浏览器打开的页面列表
2. `select_page` → 选中目标页面（pageId + bringToFront: true）
3. **默认选择浏览器中最后一个标签页**，确认其域名为 `{国家代码}.shopify.com`

### 修改后验证流程

每次修改代码后，按以下步骤验证：

1. **刷新页面**: `navigate_page` → type: "reload", ignoreCache: true
2. **截图确认**: `take_screenshot` → 确认视觉效果
3. **DOM 检查**: `take_snapshot` → 检查元素结构和属性
4. **JS 验证**: `evaluate_script` → 验证 JS 逻辑和数据

### 响应式测试

- **PC 端**: 默认视口，直接截图
- **移动端**: `emulate` → viewport: "375x812x3,mobile,touch"（iPhone 尺寸）
- **平板**: `emulate` → viewport: "768x1024x2,mobile,touch"
- **重置**: `emulate` → viewport: "1440x900x1"

### 性能与质量审计

- **Lighthouse**: `lighthouse_audit` → mode: "navigation", device: "desktop"/"mobile"
- **网络请求**: `list_network_requests` → 检查 API 和资源加载
- **控制台错误**: `list_console_messages` → 检查 JS 错误

### 交互测试

- **点击元素**: `click` → 传入 snapshot 中的 uid
- **填写表单**: `fill` / `fill_form` → 测试表单交互
- **键盘操作**: `press_key` / `type_text` → 模拟用户输入

## Debug 埋点规范

- **禁止使用 fetch 埋点**：Shopify 预览链接不支持 fetch 请求，无法用于调试
- 使用其他方式（如 console.log、DOM 标记）进行调试

## Shopify 文档查询规范

- **必须使用 Shopify MCP 工具**查询任何 Shopify 相关问题和文档
- **查询流程**：
  1. 使用 `shopify-dev-mcp_learn_shopify_api` 加载对应的 API（如 admin、storefront、functions 等）
  2. 使用 `shopify-dev-mcp_search_docs_chunks` 搜索相关文档
  3. 如需验证 GraphQL 代码，使用 `shopify-dev-mcp_validate_graphql_codeblocks`
- **禁止使用网络搜索**：不要使用 websearch 或 webfetch 工具查询 Shopify 文档，MCP 工具提供的信息更准确

## Section Schema 配置文档自动生成

当新建或更新 `sections/*.liquid` 文件中的 schema 配置时，**自动生成配置说明并同步到飞书文档**。

### 执行流程

1. **解析 Schema**：从 section 文件中提取 schema JSON，解析 settings、blocks、presets
2. **生成 Markdown**：生成配置说明文档，包含设置项表格、区块类型说明、预设配置
3. **同步飞书**：
   - 使用 `feishu-mcp_docx_builtin_search` 搜索是否已有该 section 的配置文档
   - 如有，更新现有文档；如无，使用 `feishu-mcp_docx_builtin_import` 创建新文档
4. **告知用户**：完成后告知用户飞书文档链接

### 文档格式

```markdown
# Section 名称配置说明

## 设置项 (settings)

| 名称 | 类型 | 默认值 | 说明 |
|------|------|--------|------|

## 区块类型 (blocks)

### block-type-name
- 描述: ...

## 预设配置 (presets)
```

### 文档命名规范

`[Section] 配置说明 - {section-name}`

## 代码审查要点

1. 检查是否使用 Shopify 2.0 特性（sections、blocks、schema）
2. 确保响应式设计适配移动端
3. 检查图片是否使用懒加载 `loading="lazy"`
4. 验证多语言翻译 key 存在
5. 确认未修改第三方应用管理的文件
6. 新增样式是否在 `custom.css` 或独立文件中
7. git diff 主要为新增行，非修改行
